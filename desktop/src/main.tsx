import React, { useEffect, useRef, useState } from 'react';
import { createRoot } from 'react-dom/client';
import { Settings } from 'lucide-react';
import { api, type Project, type Task } from './api';
import { SettingsCenter } from './components/SettingsCenter/SettingsCenter';
import { Workspace } from './pages/Project/Workspace';
import { ProjectHome } from './pages/Project/ProjectHome';
import { localizedMessage, statusLabel } from './lib/crossborderLabels';
import './styles.css';

function ProjectDialog({ open, busy, error, onClose, onCreate }: { open: boolean; busy: boolean; error: string; onClose: () => void; onCreate: (name: string) => Promise<void> }) {
  const [name, setName] = useState('');
  const inputRef = useRef<HTMLInputElement>(null);
  useEffect(() => { if (open) { setName(''); window.setTimeout(() => inputRef.current?.focus(), 0); } }, [open]);
  useEffect(() => { if (!open) return; const onKeyDown = (event: KeyboardEvent) => { if (event.key === 'Escape' && !busy) onClose(); }; window.addEventListener('keydown', onKeyDown); return () => window.removeEventListener('keydown', onKeyDown); }, [busy, onClose, open]);
  if (!open) return null;
  return <div className="dialog-backdrop" role="presentation" onMouseDown={(event) => { if (event.target === event.currentTarget && !busy) onClose(); }}><form className="project-dialog" onSubmit={(event) => { event.preventDefault(); if (name.trim()) void onCreate(name.trim()); }}><div className="dialog-heading"><div><span className="kicker">新建工作区</span><h2>创建商品目录项目</h2></div><button type="button" className="icon-button" onClick={onClose} disabled={busy} aria-label="关闭">×</button></div><p className="dialog-copy">集中管理供应商资料、商品事实、合规审核、平台草稿和导出文件。</p><label className="dialog-field"><span>项目名称</span><input ref={inputRef} value={name} onChange={(event) => setName(event.target.value)} placeholder="例如：夏季女装美国市场" maxLength={120} /></label>{error && <p className="error">{error}</p>}<div className="dialog-actions"><button type="button" onClick={onClose} disabled={busy}>取消</button><button className="primary" type="submit" disabled={busy || !name.trim()}>{busy ? '创建中…' : '创建项目'}</button></div></form></div>;
}

function App() {
  const [health, setHealth] = useState('checking'); const [projects, setProjects] = useState<Project[]>([]); const [tasks, setTasks] = useState<Task[]>([]); const [selectedProject, setSelectedProject] = useState<Project | null>(null); const [selectedTask, setSelectedTask] = useState<string | null>(null); const [message, setMessage] = useState(''); const [dialogOpen, setDialogOpen] = useState(false); const [dialogBusy, setDialogBusy] = useState(false); const [dialogError, setDialogError] = useState(''); const [settingsOpen, setSettingsOpen] = useState(false); const refreshRequestRef = useRef(0);
  async function refresh(projectId?: string, preferredTaskId?: string | null) { const requestId = ++refreshRequestRef.current; try { const identity = await api.health(); if (identity.app_id !== 'crossborder-catalog-cowork' || identity.protocol_name !== 'agentteams' || identity.protocol_version !== 1) throw new Error('桌面端与后端版本不兼容。'); const projectResult = await api.projects(); const activeId = projectId || selectedProject?.id || projectResult.items[0]?.id; const active = projectResult.items.find((item) => item.id === activeId) || null; const nextTasks = active ? (await api.tasks(active.id)).items : []; if (requestId !== refreshRequestRef.current) return; setHealth('online'); setMessage(''); setProjects(projectResult.items); const projectChanged = active?.id !== selectedProject?.id; setSelectedProject(active); setTasks(nextTasks); const requestedTask = preferredTaskId === undefined ? (projectChanged ? null : selectedTask) : preferredTaskId; setSelectedTask(requestedTask && nextTasks.some((task) => task.id === requestedTask) ? requestedTask : null); } catch (reason) { if (requestId !== refreshRequestRef.current) return; setHealth('offline'); setMessage(localizedMessage(reason)); } }
  useEffect(() => { void refresh(); }, []);
  async function createProject(name: string) { setDialogBusy(true); setDialogError(''); try { const result = await api.createProject(name); setDialogOpen(false); await refresh(result.project.id, null); } catch (reason) { setDialogError(localizedMessage(reason)); } finally { setDialogBusy(false); } }
  async function createTask(objective: string, materialIds: string[]) { if (!selectedProject) return; const result = await api.createTask(selectedProject.id, objective, materialIds); await api.runTask(result.task.id); await refresh(selectedProject.id, result.task.id); }
  return <div className="app-shell"><header className="topbar"><div className="brand-mark">C</div><div className="brand-copy"><strong>跨境商品协作平台</strong><span>商品目录治理工作区</span></div><div className="topbar-actions"><span className={`health ${health}`}><i /> 服务 {statusLabel(health)}</span><button className="topbar-button" onClick={() => setSettingsOpen(true)} aria-label="打开设置" title="设置"><Settings size={16} /></button><button className="topbar-button" onClick={() => void refresh(selectedProject?.id, selectedTask)} aria-label="刷新">↻</button></div></header><main className="workspace-layout"><aside className="history-rail"><div className="rail-heading"><div><span className="rail-label">工作区</span><h2>项目</h2></div><button className="new-project-button" onClick={() => { setDialogError(''); setDialogOpen(true); }} aria-label="新建项目">+</button></div><div className="project-list">{projects.map((project) => <button className={`project-row ${selectedProject?.id === project.id ? 'selected' : ''}`} key={project.id} onClick={() => void refresh(project.id, null)}><span className="project-avatar">{project.name.slice(0, 1).toUpperCase()}</span><span className="project-row-copy"><strong>{project.name}</strong><small>{project.id.slice(-8)}</small></span><span className="row-chevron">›</span></button>)}{!projects.length && <div className="rail-empty"><span>＋</span><p>还没有项目</p><button onClick={() => setDialogOpen(true)}>创建第一个项目</button></div>}</div>{selectedProject && <><div className="rail-section-heading"><span>最近任务</span><small>{tasks.length}</small></div><div className="rail-task-list">{tasks.map((task) => <button className={`task-row ${selectedTask === task.id ? 'selected' : ''}`} key={task.id} onClick={() => setSelectedTask(task.id)}><span className={`task-dot ${task.status}`} /><span className="task-row-copy"><strong>{task.objective}</strong><small>{statusLabel(task.status)}</small></span></button>)}{!tasks.length && <p className="rail-hint">先添加项目素材，再创建任务。</p>}</div></>}<div className="rail-footer"><span className="rail-label">目标平台</span><span>美国 · Shopify · eBay</span></div></aside><section className="main-stage">{selectedTask && selectedProject ? <Workspace taskId={selectedTask} onRefreshTasks={() => refresh(selectedProject.id, selectedTask)} onBackToProject={() => void refresh(selectedProject.id, null)} /> : selectedProject ? <ProjectHome project={selectedProject} tasks={tasks} onCreateTask={createTask} onOpenTask={setSelectedTask} /> : <section className="welcome-state"><div className="welcome-orbit">✦</div><span className="kicker">跨境商品目录治理</span><h1>将原始资料转化为可交付的商品目录包。</h1><p>创建工作区，统一管理商品事实、美国合规检查、平台草稿、人工审批和可审计的导出文件。</p><button className="primary large" onClick={() => setDialogOpen(true)}>创建项目 <span>→</span></button>{message && <p className="error">{message}</p>}</section>}</section></main><ProjectDialog open={dialogOpen} busy={dialogBusy} error={dialogError} onClose={() => setDialogOpen(false)} onCreate={createProject} /><SettingsCenter open={settingsOpen} onClose={() => setSettingsOpen(false)} /></div>;
}

createRoot(document.getElementById('root')!).render(<React.StrictMode><App /></React.StrictMode>);
